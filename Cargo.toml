[workspace]
members = [
    "utils",
    "archival",
    "eh_bot",
]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html
[profile.release]
# strip = true
opt-level = 3
lto = true
codegen-units = 1
